import { CheckCircle2, Clock3 } from 'lucide-react'
import { Link, useLocation, useParams } from 'react-router-dom'
import { Button } from '@/components/ui/button'
import { getBooking } from '@/services/bookingService'
import { useEffect, useState } from 'react'

export default function BookingSuccessPage() { const { id } = useParams(); const { state } = useLocation(); const [booking, setBooking] = useState(state?.booking || null); useEffect(() => { if (!booking) getBooking(id).then(setBooking).catch(() => {}) }, [booking, id]); return <div className="flex min-h-[75vh] items-center bg-slate-50/60 px-4 pt-20"><div className="mx-auto w-full max-w-lg rounded-2xl border border-slate-100 bg-white p-7 text-center shadow-[0_16px_40px_rgba(15,23,42,0.08)] sm:p-10"><div className="mx-auto flex size-16 items-center justify-center rounded-full bg-rideon-green/15"><CheckCircle2 className="size-9 text-rideon-green" /></div><p className="mt-5 text-sm font-bold uppercase tracking-[.16em] text-rideon-green">Reservation created</p><h1 className="mt-2 text-3xl font-extrabold text-rideon-dark">Payment pending</h1><p className="mt-3 text-sm leading-6 text-slate-500">Your booking is reserved. Payment will be available here soon; no charge has been made today.</p>{booking && <div className="mt-6 rounded-xl bg-slate-50 p-4 text-left"><p className="text-xs font-semibold text-slate-400">BOOKING REFERENCE</p><p className="mt-1 font-extrabold text-rideon-dark">{booking.bookingNumber}</p><p className="mt-3 flex items-center gap-2 text-sm text-slate-600"><Clock3 className="size-4 text-rideon-blue" />{new Date(booking.pickupAt).toLocaleString([], { dateStyle: 'medium', timeStyle: 'short' })}</p></div>}<div className="mt-7 grid gap-3 sm:grid-cols-2"><Button variant="outline" className="border-rideon-blue text-rideon-blue" asChild><Link to="/booking">Book another ride</Link></Button><Button className="bg-rideon-blue text-white" asChild><Link to={`/bookings/${id}`}>View booking</Link></Button></div></div></div> }
